package a2;

import java.util.Scanner;

public class A2Jedi {

	public static void main(String[] args) {
		Scanner scan = new Scanner(System.in);
		/*int ingredients = scan.nextInt();

		String[] name = new String[ingredients];
		double[] price = new double[ingredients];
		boolean[] vegOrNot = new boolean[ingredients];
		double[] calories = new double[ingredients];

		for (int i = 0; i < ingredients; i++) {
			name[i] = scan.next();
			price[i] = scan.nextDouble();
			vegOrNot[i] = scan.nextBoolean();
			calories[i] = scan.nextDouble();
		}

		int menuItems = scan.nextInt();

		String[] menuName = new String[menuItems];


		double totalCalories = 0;
		double totalCost = 0;
		String totalVeg = null;
		double calSum = 0;
		double costSum = 0;

		for (int i = 0; i < menuItems; i++) { //for every menu item
			menuName[i] = scan.next();
			int numberOfIngredients = scan.nextInt();

			String[] ingredientName = new String[numberOfIngredients];
			double[] ounces = new double[numberOfIngredients];

			for (int j = 0; j < numberOfIngredients; j++) { //for every ingredient in every menu item
				ingredientName[j] = scan.next();
				ounces[j] = scan.nextDouble();
			}
		}
		ArrayList<String> menuOrders = new ArrayList<>();

		String order = null;

		while(order != "EndOrder") {
			menuOrders.add(order = scan.nextLine());
		}

		ArrayList<String> menuRequirements = new ArrayList<>();
		ArrayList<String> ingredientRequired = new ArrayList<>();
		ArrayList<Double> ouncesRequired = new ArrayList<>();
		double ouncesReq =0;
		String ingredientReq = null;
		for(int i=0; i<menuOrders.size();i++){
			for(int j=0; j<menuName.length;j++){
				if(menuName[j].equals(menuOrders.get(i))){
					//for(int k=0; k<ingredientName.clone.size();)

					ingredientRequired.add(ingredientReq);
					ouncesRequired.add(ouncesReq);

					break;
				}
			}
		}


		System.out.println("This order will require: ");
		for(int i=0; i<menuRequirements.size();i++){
			//(ouncesRequired.get(i)+" ounces of "+ingredientRequired.get(i));
		}*/

		// Your code here.
	}
	
	// You can define helper methods here if needed.
	
}
